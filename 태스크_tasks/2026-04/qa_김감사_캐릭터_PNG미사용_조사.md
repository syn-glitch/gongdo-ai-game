# QA-2026-04-19-007 — 캐릭터 변경 효과 0 (PNG 미사용) 긴급 조사

---
- **QA ID**: QA-2026-04-19-007
- **요청일**: 2026-04-19
- **담당팀**: 🕵️ 김감사 QA팀
- **검수 대상**: chat.js v1.4.0 SYSTEM_GENERATOR + 운영 게임 생성 결과
- **검수 방식**: 라이브 4-캐릭터 매트릭스 + curl PNG 배포 확인 + chat.js prompt 분석
- **판정**: ❌ **반려** — CRITICAL 1건 즉시 fix 필요
- **CRITICAL**: 1건 / MAJOR: 1건 / MINOR: 0건
---

## 요청 원문
> 캐릭터 선택시 변경이 안됨
> - 캐릭터 밥 선택 → 토끼만 나옴
> - 코드보기에서 캐릭터 코드 → 밥이 안되어 있는것 같음
> 밥/ㅋㅋ/토리/레옹 모두 반영되야 필수

## 결론 (한 줄)
**chat.js SYSTEM_GENERATOR 의 "임시 에셋 폴백" 블록이 PNG 배포 후에도 살아있어
AI 가 4개 캐릭터 PNG 를 100% 사용하지 않고 이모지만 생성.** 학습 핵심 가치 훼손.

---

## 진단 결과 (7축)

### ① 4개 PNG 파일 배포 상태 — ✅ 정상
| 파일 | HTTP | 크기 |
|------|------|------|
| kk_idle.png | 200 | 47,331 bytes |
| tory_idle.png | 200 | 69,475 bytes |
| bob_idle.png | 200 | 50,006 bytes |
| leon_idle.png | 200 | 42,299 bytes |

→ PNG 모두 정상 배포. **사용 안 하는 게 문제.**

### ②④ 4-캐릭터 매트릭스 — 🚨 100% PNG 미사용
| 캐릭터 선택 | imgTags | drawImage | fillTextEmoji | PNG URL 코드에 |
|-------------|---------|-----------|---------------|----------------|
| ㅋㅋ | **0** | **0** | 2 | NONE |
| 토리 | **0** | **0** | 2 | NONE |
| 밥 | **0** | **0** | 2 | NONE |
| 레옹 | **0** | **0** | 2 | NONE |

→ 4개 캐릭터 어떤 걸 선택해도 **AI 는 항상 fillText 이모지만 사용**.

### ③ chat.js Prompt 분석 — 🚨 충돌하는 stale 블록 발견
**규칙 0 (line 36~72):** "이미지 URL 명시되면 → **반드시 이미지 사용**"
↓
**임시 에셋 폴백 (line 191~198):** "현재 캐릭터 PNG 파일은 아직 준비 중...
**캐릭터는 반드시 이모지로 렌더링 하는 코드를 기본**으로 작성"
↓
**케이스 A (line 200~213):** drawPlayer 예시도 `fillText('🦸')` 만 보여줌

→ **모순**. AI Claude Haiku 가 후자 (강한 명령어 + 예시 코드) 채택 → 항상 이모지.
→ "PNG 파일은 아직 준비 중" 이라는 문구는 **사실과 다름** (PNG 모두 200 OK 배포됨).
→ stale prompt 로 운영 중.

### ⑤ 코드 보기 매핑 — ⚠️ player 객체만 매칭
- 주인공 [🔍 코드] 클릭 → 9 라인 매칭
- 매칭 범위: `// 📝 "주인공: 토리"` + `const player = { x, y, w, h, speed, vx }` 객체
- **drawPlayer() 함수 (실제 그리기 부분) 는 매칭 X**
- 학생이 "여기 어디에 밥이 그려져?" 물으면 → "위치·크기만 있고 그림 코드는 없네" 오인

### ⑥ 시각적 fillText 샘플
```js
fillText('👒', player.x, player.y)  // 토리 → 모자 이모지 (PNG 아님)
fillText('👾', e.x, e.y)            // 적 → 외계인 이모지
fillText(`점수: ${score}/10`, ...)
```

### ⑦ 회귀 — 무영향
- 변경 X — 본 건은 신기능이 아닌 기존 prompt 의 stale 문제

---

## 근본 원인 (Root Cause)

```
chat.js SYSTEM_GENERATOR (v1.4.0)
├── 규칙 0: 이미지 URL → drawImage 사용 (정답)
├── 임시 에셋 폴백: "PNG 준비 중... 이모지로 렌더링 기본" (STALE — fact 와 다름)
└── 케이스 A 예시: fillText('🦸') 만 (잘못된 가이드)

AI Haiku 가 후자 둘 (더 구체적·반복 강조) 채택
→ 항상 이모지, drawImage 0건
```

**메모리 확인**: 사용자 메모리 "최AR 투입 시점 — S13 완료 후" — PNG 추출 작업이 진행 중이던 시점에 작성된 prompt. 현재는 PNG 모두 배포됨. **prompt 가 시대 지났음**.

---

## 이슈 분류

### 🔥 CRITICAL — chat.js prompt stale (캐릭터 차별화 0)
- 학습 핵심 가치 (학생 IP 캐릭터 활용 → 친밀감 + 게임 몰입) 훼손
- 4개 캐릭터 모두 이모지 폴백만 사용 → 학생이 "내가 고른 캐릭터" 인지 약화
- 즉시 fix 필요

### ⚠️ MAJOR — 주인공 코드 매핑이 player 객체만
- 학생이 "내 주인공 그리는 코드" 찾으려 [🔍 코드] 클릭 → player 객체 (위치·크기) 만 매칭
- drawPlayer 함수 (실제 그리기) 는 별도 // 📝 주석이 없으면 매칭 X
- 사용자 인지: "그림 코드가 안 보임" 오인 가능
- 매핑 알고리즘 또는 AI 주석 패턴 강화 권장

---

## 권장 조치 (자비스 위임)

### 즉시 fix — chat.js prompt 정정
1. **"임시 에셋 폴백" 블록 (line 190~198) 제거 또는 약화**
   - 현재: "PNG 아직 준비 중... 이모지 기본"
   - 변경: "이미지 URL 있으면 drawImage 우선 + onerror 시 이모지 폴백"
2. **케이스 A 예시 갱신**
   - 현재: `function drawPlayer(...) { ctx.fillText('🦸', ...) }`
   - 변경: 이미지 프리로드 + drawImage 패턴 (이미 규칙 0 ① 에 정답 있음 — 케이스 A 가 그 패턴 따라야)
3. **규칙 0 우선순위 강조**
   - "임시 폴백" 블록 보다 위에 두고 "이 규칙이 절대 우선" 명시

### 선택 — 코드 매핑 강화 (별도 task)
- AI 주석에 `// 📝 "주인공: ㅋㅋ"` 외에 drawPlayer 위에도 `// 📝 "주인공 그리기" → 너의 주인공을 화면에 그리는 묶음` 추가하도록 prompt 보강
- OR 매핑 알고리즘이 sectionTitle "주인공" 클릭 시 drawPlayer 함수까지 자동 검색 (정적 키워드)

---

## 위임 안내 🔀

```
──────────────────────────
🔀 업무 위임 안내 (긴급)
──────────────────────────
발견 내용: chat.js v1.4.0 SYSTEM_GENERATOR 의 "임시 에셋 폴백" 블록이 stale.
         PNG 4종 모두 배포됐는데도 AI 가 100% 이모지만 사용.
         학생이 캐릭터 4종 어떤 걸 선택해도 차별화 0.

이 작업의 담당 팀:
  🥇 1차 (즉시): 🤵 자비스 PO → 👨‍💻 알렉스 TL (chat.js prompt 재설계)
                + 👩‍💻 에이다 BE (chat.js 응답 검증)
  🥈 2차 (선택): 매핑 알고리즘 강화 (별도 task)

권장 조치:
  1. chat.js v1.4.0 → v1.5.0
  2. SYSTEM_GENERATOR 의 "임시 에셋 폴백" 블록 제거 또는 약화
  3. 케이스 A 예시를 drawImage 패턴으로 갱신
  4. 라이브 검증 (4-캐릭터 매트릭스 — drawImage > 0 + img URL 코드 포함)
  5. 김감사 재검수 의뢰

관련 파일:
  - 앱_app/api/chat.js (SYSTEM_GENERATOR, line 36~224)

우선순위: 🔥 긴급 (CRITICAL — 학습 핵심 가치 훼손, 학생 시연 직접 영향)
──────────────────────────
```

---

## 김감사 QA팀 역할 경계 준수 ✅
본 보고서는 **판정·진단·위임**만 수행. chat.js 수정은 자비스 알렉스 TL 소관.

## 변경 이력
| 날짜 | 변경 내용 | 변경자 |
|------|----------|--------|
| 2026-04-19 | 최초 생성 — 4-캐릭터 매트릭스 + chat.js prompt 분석 (CRITICAL 1 / MAJOR 1) | 🕵️ 김감사 |
